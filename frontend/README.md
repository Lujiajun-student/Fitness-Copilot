# Vue 2 前端

Vue 2 用户界面工程。当前包含运动风应用壳、当日计划首页、私人助理聊天页与个人信息页；个人信息页支持头像本地预览、账户资料编辑、身体数据隐藏/展示与编辑。

开发时，Vue CLI 会将 `/api` 转发到 `http://localhost:8080`。登录、注册、个人资料和身体数据已连接后端；先启动 Java 后端，再执行前端服务。

```powershell
npm install
npm run serve
```
